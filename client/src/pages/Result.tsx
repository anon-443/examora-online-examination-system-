import { SiteFooter } from "@/components/SiteFooter";
import { SiteHeader } from "@/components/SiteHeader";
import { StudentGate } from "@/components/StudentGate";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { ArrowRight, Award, CheckCircle2, CircleX, Loader2, RotateCcw, Trophy } from "lucide-react";
import { useRoute } from "wouter";
import { Link } from "wouter";

export default function Result() {
  const [, params] = useRoute("/results/:attemptId");
  const attemptId = Number(params?.attemptId);
  const { data: result, isLoading } = trpc.attempts.result.useQuery({ attemptId }, { enabled: attemptId > 0 });
  const success = (result?.percentage ?? 0) >= 65;
  return <div className="min-h-screen bg-[hsl(var(--page))]"><SiteHeader /><StudentGate>{isLoading ? <div className="grid min-h-[70vh] place-items-center"><Loader2 className="size-7 animate-spin text-emerald-600" /></div> : !result ? <div className="container py-24 text-center"><h1 className="font-display text-3xl font-semibold dark:text-white">Result unavailable</h1><Button asChild className="mt-5 rounded-xl"><Link href="/history">Visit my activity</Link></Button></div> : <main className="container py-12 sm:py-20"><div className="mx-auto max-w-3xl"><div className={`overflow-hidden rounded-[2rem] border p-7 text-center shadow-[0_30px_80px_-40px_rgba(15,23,42,0.35)] sm:p-11 ${success ? "border-emerald-200 bg-gradient-to-b from-emerald-50 to-white dark:border-emerald-400/20 dark:from-emerald-400/10 dark:to-slate-900" : "border-amber-200 bg-gradient-to-b from-amber-50 to-white dark:border-amber-400/20 dark:from-amber-400/10 dark:to-slate-900"}`}><span className={`mx-auto grid size-16 place-items-center rounded-2xl ${success ? "bg-emerald-600 text-white" : "bg-amber-500 text-white"}`}>{success ? <Trophy className="size-7" /> : <Award className="size-7" />}</span><p className="mt-6 text-xs font-bold uppercase tracking-[0.16em] text-slate-500 dark:text-slate-400">Assessment complete</p><h1 className="mt-3 font-display text-3xl font-semibold tracking-[-0.055em] text-slate-950 sm:text-4xl dark:text-white">{result.examTitle}</h1><p className="mx-auto mt-4 max-w-xl text-sm leading-6 text-slate-600 dark:text-slate-300">{result.summary}</p><div className="mx-auto mt-9 grid max-w-md grid-cols-3 divide-x divide-slate-200 rounded-2xl border border-slate-200 bg-white/80 py-5 dark:divide-white/10 dark:border-white/10 dark:bg-slate-950/40"><div><p className="font-display text-3xl font-semibold tracking-[-0.06em] text-slate-950 dark:text-white">{result.percentage}%</p><p className="mt-1 text-[10px] font-bold uppercase tracking-wider text-slate-500">Score</p></div><div><p className="font-display text-3xl font-semibold tracking-[-0.06em] text-emerald-600 dark:text-emerald-400">{result.score}</p><p className="mt-1 text-[10px] font-bold uppercase tracking-wider text-slate-500">Correct</p></div><div><p className="font-display text-3xl font-semibold tracking-[-0.06em] text-rose-600 dark:text-rose-400">{result.incorrectAnswers}</p><p className="mt-1 text-[10px] font-bold uppercase tracking-wider text-slate-500">Incorrect</p></div></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><div className="rounded-2xl border border-slate-200 bg-white p-5 dark:border-white/10 dark:bg-slate-900"><p className="flex items-center gap-2 text-sm font-bold dark:text-white"><CheckCircle2 className="size-4 text-emerald-600" />Results saved</p><p className="mt-2 text-sm leading-6 text-slate-600 dark:text-slate-400">Your completed assessment is now available in your activity history.</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5 dark:border-white/10 dark:bg-slate-900"><p className="flex items-center gap-2 text-sm font-bold dark:text-white"><CircleX className="size-4 text-amber-500" />Keep growing</p><p className="mt-2 text-sm leading-6 text-slate-600 dark:text-slate-400">Try a different assessment or revisit this subject when you are ready.</p></div></div><div className="mt-7 flex flex-col justify-center gap-3 sm:flex-row"><Button asChild className="rounded-xl bg-slate-950 text-white hover:bg-slate-800 dark:bg-white dark:text-slate-950"><Link href="/history">View my activity <ArrowRight className="ml-1.5 size-4" /></Link></Button><Button asChild variant="outline" className="rounded-xl"><Link href="/exams"><RotateCcw className="mr-1.5 size-4" />Explore more assessments</Link></Button></div></div></main>}</StudentGate><SiteFooter /></div>;
}
