import { SiteFooter } from "@/components/SiteFooter";
import { SiteHeader } from "@/components/SiteHeader";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { ArrowRight, CalendarDays, Crown, Medal, Sparkles, Trophy } from "lucide-react";
import { useMemo, useState } from "react";
import { Link } from "wouter";

const medals = ["bg-amber-400 text-amber-950", "bg-slate-300 text-slate-700", "bg-orange-200 text-orange-800"];
const periods = [
  { id: "all", label: "All time" },
  { id: "weekly", label: "This week" },
  { id: "monthly", label: "This month" },
] as const;

export default function Leaderboard() {
  const [subject, setSubject] = useState("");
  const [period, setPeriod] = useState<(typeof periods)[number]["id"]>("all");
  const { data: rankingSubjects = [] } = trpc.attempts.leaderboardSubjects.useQuery();
  const subjects = useMemo(() => rankingSubjects, [rankingSubjects]);
  const { data: rows = [], isLoading } = trpc.attempts.leaderboard.useQuery({ subject, period });
  const periodLabel = periods.find(item => item.id === period)?.label.toLowerCase() ?? "all time";
  return <div className="min-h-screen bg-[hsl(var(--page))]"><SiteHeader /><main><section className="relative overflow-hidden border-b border-slate-200 bg-slate-950 py-8 text-white sm:py-10 dark:border-white/10"><div className="absolute -right-20 -top-20 size-80 rounded-full bg-emerald-400/15 blur-3xl" /><div className="container relative text-center"><span className="mx-auto grid size-10 place-items-center rounded-2xl bg-white/10 text-amber-300"><Trophy className="size-5" /></span><p className="mt-3 text-xs font-bold uppercase tracking-[0.16em] text-emerald-300">Focus recognised</p><h1 className="mt-2 font-display text-4xl font-semibold tracking-[-0.06em] sm:text-5xl">The Examora leaderboard</h1><p className="mx-auto mt-3 max-w-none text-sm leading-6 text-slate-300 xl:whitespace-nowrap">Recognising strong completed assessment performance by subject and recent progress</p></div></section><section className="container py-6 sm:py-8"><div className="mx-auto max-w-3xl"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-white/10 dark:bg-slate-900"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-emerald-700 dark:text-emerald-400">Ranking filters</p><h2 className="mt-1 font-display text-xl font-semibold tracking-[-0.035em] text-slate-950 dark:text-white">See performance in context</h2></div><label className="text-xs font-bold text-slate-500 dark:text-slate-400">Subject<select value={subject} onChange={event => setSubject(event.target.value)} className="mt-1.5 h-11 w-full min-w-44 rounded-lg border border-slate-200 bg-white px-3 text-sm font-semibold text-slate-700 outline-none focus:ring-2 focus:ring-emerald-500 sm:w-auto dark:border-white/10 dark:bg-white/5 dark:text-white"><option value="">All subjects</option>{subjects.map(item => <option key={item} value={item}>{item}</option>)}</select></label></div><div className="mt-5 flex flex-wrap gap-2">{periods.map(item => <button key={item.id} onClick={() => setPeriod(item.id)} className={`inline-flex min-h-10 items-center gap-1.5 rounded-full border px-3 py-2 text-xs font-bold transition-colors ${period === item.id ? "border-emerald-600 bg-emerald-600 text-white" : "border-slate-200 bg-slate-50 text-slate-600 hover:border-emerald-300 hover:text-emerald-700 dark:border-white/10 dark:bg-white/[0.03] dark:text-slate-300"}`}><CalendarDays className="size-3.5" />{item.label}</button>)}</div></section>{isLoading ? <div className="mt-6 h-72 animate-pulse rounded-3xl bg-slate-100 dark:bg-white/5" /> : rows.length ? <div className="mt-6 overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-[0_24px_55px_-40px_rgba(15,23,42,0.35)] dark:border-white/10 dark:bg-slate-900"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-5 sm:px-6 dark:border-white/10"><div><p className="font-display text-xl font-semibold tracking-[-0.035em] dark:text-white">Top performers</p><p className="mt-1 text-xs text-slate-500 dark:text-slate-400">{subject || "All subjects"} · {periodLabel} · ranked by percentage, then score</p></div><Crown className="size-6 text-amber-500" /></div>{rows.map((row, index) => <div key={`${row.rank}-${row.name}-${row.subject}`} className="flex items-center gap-3 border-b border-slate-100 px-4 py-4 last:border-0 sm:gap-4 sm:px-6 dark:border-white/10"><span className={`grid size-9 shrink-0 place-items-center rounded-xl text-sm font-bold ${index < 3 ? medals[index] : "bg-slate-100 text-slate-500 dark:bg-white/10 dark:text-slate-300"}`}>{index < 3 ? <Medal className="size-4" /> : row.rank}</span><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold text-slate-900 dark:text-white">{row.name}</p><p className="mt-0.5 truncate text-xs text-slate-500 dark:text-slate-400">Best {row.subject} assessment</p></div><div className="text-right"><p className="font-display text-xl font-semibold tracking-[-0.04em] text-slate-950 dark:text-white">{row.percentage}%</p><p className="text-[10px] font-bold uppercase tracking-wider text-slate-400">{row.score} correct</p></div></div>)}</div> : <div className="mt-6 grid min-h-64 place-items-center rounded-3xl border border-dashed border-slate-300 bg-slate-50 text-center dark:border-white/15 dark:bg-white/[0.03]"><div className="max-w-sm px-6"><span className="mx-auto grid size-12 place-items-center rounded-2xl bg-amber-50 text-amber-600 dark:bg-amber-400/10 dark:text-amber-300"><Trophy className="size-5" /></span><h2 className="mt-4 font-display text-xl font-semibold tracking-[-0.035em] text-slate-950 dark:text-white">No rankings match these filters yet</h2><p className="mt-2 text-sm leading-6 text-slate-500 dark:text-slate-400">Choose another period or subject, or complete a published assessment to establish the next ranking</p><Button asChild className="mt-5 min-h-11 rounded-xl bg-slate-950 text-white dark:bg-white dark:text-slate-950"><Link href="/exams"><Sparkles className="mr-1.5 size-4" />Explore assessments</Link></Button></div></div>}</div></section></main><SiteFooter /></div>;
}
