import { SiteFooter } from "@/components/SiteFooter";
import { SiteHeader } from "@/components/SiteHeader";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { ArrowRight, Crown, Medal, Sparkles, Trophy } from "lucide-react";
import { Link } from "wouter";

const medals = ["bg-amber-400 text-amber-950", "bg-slate-300 text-slate-700", "bg-orange-200 text-orange-800"];

export default function Leaderboard() {
  const { data: rows = [], isLoading } = trpc.attempts.leaderboard.useQuery();
  return <div className="min-h-screen bg-[hsl(var(--page))]"><SiteHeader /><main><section className="relative overflow-hidden border-b border-slate-200 bg-slate-950 py-16 text-white sm:py-20 dark:border-white/10"><div className="absolute -right-20 -top-20 size-80 rounded-full bg-emerald-400/15 blur-3xl" /><div className="container relative text-center"><span className="mx-auto grid size-12 place-items-center rounded-2xl bg-white/10 text-amber-300"><Trophy className="size-6" /></span><p className="mt-5 text-xs font-bold uppercase tracking-[0.16em] text-emerald-300">Focus recognised</p><h1 className="mt-3 font-display text-4xl font-semibold tracking-[-0.06em] sm:text-5xl">The Examora leaderboard.</h1><p className="mx-auto mt-4 max-w-xl text-sm leading-6 text-slate-300">A simple recognition of strong completed assessment performance. Each student is represented by their best recorded result.</p></div></section><section className="container py-12 sm:py-16">{isLoading ? <div className="h-80 animate-pulse rounded-3xl bg-slate-100 dark:bg-white/5" /> : rows.length ? <div className="mx-auto max-w-3xl overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-[0_24px_55px_-40px_rgba(15,23,42,0.35)] dark:border-white/10 dark:bg-slate-900"><div className="flex items-center justify-between border-b border-slate-100 px-6 py-5 dark:border-white/10"><div><p className="font-display text-xl font-semibold tracking-[-0.035em] dark:text-white">Top performers</p><p className="mt-1 text-xs text-slate-500 dark:text-slate-400">Ranked by percentage, then score.</p></div><Crown className="size-6 text-amber-500" /></div>{rows.map((row, index) => <div key={`${row.rank}-${row.name}`} className="flex items-center gap-4 border-b border-slate-100 px-5 py-4 last:border-0 sm:px-6 dark:border-white/10"><span className={`grid size-9 shrink-0 place-items-center rounded-xl text-sm font-bold ${index < 3 ? medals[index] : "bg-slate-100 text-slate-500 dark:bg-white/10 dark:text-slate-300"}`}>{index < 3 ? <Medal className="size-4" /> : row.rank}</span><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold text-slate-900 dark:text-white">{row.name}</p><p className="mt-0.5 text-xs text-slate-500 dark:text-slate-400">Best completed assessment</p></div><div className="text-right"><p className="font-display text-xl font-semibold tracking-[-0.04em] text-slate-950 dark:text-white">{row.percentage}%</p><p className="text-[10px] font-bold uppercase tracking-wider text-slate-400">{row.score} correct</p></div></div>)}</div> : <div className="mx-auto grid min-h-80 max-w-3xl place-items-center rounded-3xl border border-dashed border-slate-300 bg-slate-50 text-center dark:border-white/15 dark:bg-white/[0.03]"><div className="max-w-sm px-6"><span className="mx-auto grid size-12 place-items-center rounded-2xl bg-amber-50 text-amber-600 dark:bg-amber-400/10 dark:text-amber-300"><Trophy className="size-5" /></span><h2 className="mt-4 font-display text-xl font-semibold tracking-[-0.035em] text-slate-950 dark:text-white">The leaderboard is waiting for its first result</h2><p className="mt-2 text-sm leading-6 text-slate-500 dark:text-slate-400">Complete a published assessment to establish the first ranking.</p><Button asChild className="mt-5 rounded-xl bg-slate-950 text-white dark:bg-white dark:text-slate-950"><Link href="/exams"><Sparkles className="mr-1.5 size-4" />Explore assessments</Link></Button></div></div>}</section></main><SiteFooter /></div>;
}
